[project]
name = "station"
version = "0.1.0"
description = "Pi side: buffers dump1090 SBS-1 messages into SQLite, drains them to the gateway, and reports status"
requires-python = ">=3.11"
dependencies = []
